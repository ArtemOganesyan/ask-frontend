import { PasteDetectionDirective } from './paste-detection.directive';

describe('PasteDetectionDirective', () => {
  it('should create an instance', () => {
    const directive = new PasteDetectionDirective();
    expect(directive).toBeTruthy();
  });
});
